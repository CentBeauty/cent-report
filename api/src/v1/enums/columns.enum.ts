export enum UniqueCodeEnum {
  isUnique = 2,
  isNotUnique = 1,
}
export enum ActivesEnum {
  isActive = 2,
  isUnActive = 1,
}
export enum IfEnum {
  ALL = 1,
  ANY = 2,
}
export enum ThenEnum {
  TRUE = 1,
  FALSE = 2,
}
export enum typeOnLyVoucher {
  TRUE = 1,
  FALSE = 2,
}
export enum ApplyEnum {
  PERCENT = 1,
  PRICE = 2,
  PRODUCT = 3,
}
export enum TypesEnum {
  THAN_TOTAL_ORDER = 1,
  LESS_TOTAL_ORDER = 2,
  ORDERS_CONTAIN = 3,
  ORDERS_NOT_CONTAIN = 4,
  CATEGORY = 5,
  COMBO = 6,
}

export enum TypeVoucher {
  VOUCHER = 0,
  COMBO = 1,
}

export enum IsForCoucher {
  YES = 0,
  NO = 1,
}

export enum Status {
  APPLYING = 1,
  USED = 2,
}
